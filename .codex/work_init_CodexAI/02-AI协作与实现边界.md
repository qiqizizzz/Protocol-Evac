# 02-AI协作与实现边界

## 一、文档目的

本文档用于记录 AI 在本项目中协作编码时必须遵守的实现边界。

## 二、禁止过度安全化判空

禁止写这种把多个必要引用混在一起的总判空：

```csharp
if (m_characterController == null
    || m_animator == null
    || MoveConfig == null
    || ViewConfig == null
    || m_viewRoot == null
    || m_playerCamera == null)
{
    QLog.Error("玩家初始化失败：必要引用缺失，请检查 CharacterController、Animator、MoveConfig、ViewConfig、ViewRoot 与 PlayerCamera");
    return;
}
```

必要引用缺失时，应在具体获取位置暴露具体问题，不要额外写一个总判空兜底。

例如：

```csharp
m_characterController = this.GetOwnerComponent<CharacterController>();
m_animator = this.GetChildComponent<Animator>();
m_viewRoot = this.FindChild(VIEW_ROOT_PATH);
m_playerCamera = this.FindChildComponent<Camera>(PLAYER_CAMERA_PATH);
```

组件或节点缺失时，由 `GetOwnerComponent`、`GetChildComponent`、`FindChild`、`FindChildComponent` 等具体获取方法自己记录明确错误。

## 三、保留简洁方法签名

方法参数较少、单行可读性足够时，应优先保留单行方法签名，不要擅自改成一行一个参数的折叠格式。

推荐：

```csharp
public void Init(PlayerContext context, PlayerViewConfigSO viewConfig, Transform viewRoot, Camera playerCamera)
```

不推荐无必要地改成：

```csharp
public void Init(
    PlayerContext context,
    PlayerViewConfigSO viewConfig,
    Transform viewRoot,
    Camera playerCamera)
```

只有当参数过多、单行明显影响阅读，或当前文件已有统一的多行签名风格时，才考虑拆行。

## 四、内部 Init 不写重复防御式判空

由 `PlayerController`、`EnemyController` 或 Binder / Resolver 等内部装配流程调用的 `Init(...)`，其依赖来源应由上层初始化流程和具体获取方法保证，不要在每个 `Init(...)` 中重复添加防御式判空、`QLog.Error` 和 `return`。

不推荐：

```csharp
public void Init(PlayerStateMachine stateMachine, IReadOnlyDictionary<PlayerStateId, PlayerAnimRule.ResolveHandler> handlers)
{
    if (stateMachine == null)
    {
        QLog.Error("初始化玩家动画参数解析器失败：PlayerStateMachine 为空");
        return;
    }

    if (handlers == null)
    {
        QLog.Error("初始化玩家动画参数解析器失败：动画规则集合为空");
        return;
    }

    m_stateMachine = stateMachine;
    m_handlers = handlers;
}
```

推荐：

```csharp
public void Init(PlayerStateMachine stateMachine, IReadOnlyDictionary<PlayerStateId, PlayerAnimRule.ResolveHandler> handlers)
{
    m_stateMachine = stateMachine;
    m_handlers = handlers;
}
```

如果依赖来自场景层级、Inspector 配置、资源加载或外部输入，应在具体获取位置记录明确错误；如果只是内部模块之间的装配参数，不要层层补重复判空逻辑。

## 五、禁止擅自添加配置默认兜底

AI 不允许为了“先跑通”或避免空引用，在运行逻辑中新增 `DEFAULT_*` 常量、三元表达式或其他硬编码默认值来替代 ScriptableObject、Inspector 或设计数据。

不推荐：

```csharp
private const float DEFAULT_SPRINT_HOLD_TIME = 0.2f;

private float getSprintHoldTime()
{
    return m_inputConfig != null
        ? m_inputConfig.SprintHoldTime
        : DEFAULT_SPRINT_HOLD_TIME;
}
```

推荐：

```csharp
private float getSprintHoldTime()
{
    return m_inputConfig.SprintHoldTime;
}
```

如果配置缺失，应在上层配置校验处明确记录问题，并让依赖该配置的功能不进入、不执行或保持无效状态，不要静默使用 AI 自己补出的默认值。

只有用户明确要求，或项目既有配置类、设计文档已经确认该默认值属于正式设计数据时，才允许保留已有默认值。新增默认兜底必须先问用户。

## 六、运行期回调禁止重复配置判空

AI 不允许在 `Enter`、`Exit`、`Tick`、`Update`、`FixedUpdate`、`LateUpdate`、Transition Rule 条件函数等运行期回调里反复判断配置是否为空，尤其禁止这种写法：

```csharp
public void Tick(bool wasPressedThisFrame, bool isPressed, bool wasReleasedThisFrame)
{
    if (m_inputConfig == null)
    {
        m_context.IsSprintPressed = false;
        return;
    }

    // 高频逻辑
}
```

也禁止在状态生命周期里写这种每次进入都执行的配置判空：

```csharp
public override void Enter()
{
    if (m_normalAttackConfig == null)
    {
        QLog.Error("普通攻击配置未配置，无法进入普通攻击状态");
        m_context.IsStateFinished = true;
        return;
    }

    // 状态进入逻辑
}
```

配置缺失、Inspector 未绑定、初始化参数缺失等问题，应在初始化、装配或上层配置校验阶段暴露和处理。运行期回调默认依赖已经完成装配，只执行核心运行逻辑。

如果某个功能必须在配置缺失时禁用，应在初始化阶段设置一次性可用标记，或由上层不注册对应规则/状态/解释器，不要在运行期回调里重复判空。

## 七、一次性动作动画导入注意事项

闪避、翻滚、攻击、技能等一次性动作动画如果在切入时出现一帧闪动、模型瞬间转向或姿势跳变，优先检查动画导入设置，不要先怀疑 HFSM 或位移代码。

已验证案例：

```text
Dodge.fbx
└─ Root Transform Rotation
   ├─ Bake Into Pose: 开启
   ├─ Based Upon: Body Orientation
   └─ Offset: 0
```

本项目中 `Dodge.fbx` 的翻滚动画开启 `Root Transform Rotation / Bake Into Pose` 后，解决了闪避进入瞬间的一帧闪动问题。

经验规则：

```text
一次性动作动画
├─ 优先关闭 Loop Time / Loop Pose
├─ Root Transform Rotation 优先尝试 Bake Into Pose
├─ Root Transform Position (Y / XZ) 是否 Bake Into Pose 按动画资源表现决定
└─ 如果仍闪动，再检查 clip 起始帧是否有突兀预备姿势，必要时裁掉第 0~1 帧
```

代码层面仍保持由 `PlayerAnimWriter` 发起一次性播放请求，不使用 Animator Trigger 作为主流程；动画资源的根运动与根旋转问题应优先在 Import Settings 中处理。
